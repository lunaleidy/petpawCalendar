<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E8F8F5"
    tools:context=".ReenviarActivacionActivity">

    <!-- “App bar” personalizada -->
    <LinearLayout
        android:id="@+id/topBarReenviar"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_margin="5dp"
        android:background="@drawable/bg_form_card"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingVertical="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Botón atrás -->
        <LinearLayout
            android:id="@+id/btnAtrasReenviar"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/bg_input_field"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            android:paddingVertical="9dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="@string/icono_atras"
                android:src="@android:drawable/ic_media_previous"
                app:tint="#555555" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginStart="2dp"
                android:fontFamily="@font/poppinsmedium"
                android:text="@string/atras_btn"
                android:textColor="#555555"
                android:textSize="15sp" />
        </LinearLayout>

        <!-- Título centrado -->
        <TextView
            android:id="@+id/txtTituloReenviar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fontFamily="@font/poppinssemibold"
            android:gravity="center"
            android:paddingTop="5dp"
            android:text="@string/reenviar_titulo"
            android:textColor="#48C9B0"
            android:textSize="20sp"
            app:layout_constraintHorizontal_weight="1" />
    </LinearLayout>

    <!-- Tarjeta del formulario -->
    <LinearLayout
        android:id="@+id/formCardReenviar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:background="@drawable/bg_form_card"
        android:orientation="vertical"
        android:paddingTop="20dp"
        android:paddingBottom="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/topBarReenviar">

        <!-- Títulos -->
        <TextView
            android:id="@+id/txtSubtituloReenviar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppinssemibold"
            android:gravity="center"
            android:text="@string/reenviar_subtitulo"
            android:textColor="#E67E22"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/txtDescripcionReenviar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="24dp"
            android:fontFamily="@font/nunitoregular"
            android:gravity="center"
            android:text="@string/reenviar_descripcion"
            android:textColor="#7F8C8D"
            android:textSize="15sp" />

        <!-- Label Email -->
        <TextView
            android:id="@+id/lblEmailReenviar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppinsmedium"
            android:text="@string/email_label"
            android:textColor="#333333"
            android:textSize="15sp" />

        <!-- Campo Email -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/bg_input_field"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="@string/icono_email"
                android:src="@android:drawable/ic_dialog_email"
                app:tint="#B0B0B0" />

            <EditText
                android:id="@+id/edtEmailReenviar"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:autofillHints="emailAddress"
                android:background="@android:color/transparent"
                android:fontFamily="@font/nunitoregular"
                android:hint="@string/email_hint"
                android:inputType="textEmailAddress"
                android:maxWidth="320dp"
                android:padding="5dp"
                android:textColor="#333333"
                android:textColorHint="#B0B0B0"
                android:textSize="15sp"
                tools:ignore="TouchTargetSizeCheck" />
        </LinearLayout>

        <!-- Botón enviar código -->
        <Button
            android:id="@+id/btnEnviarCodigoReenviar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_btn_primary"
            android:fontFamily="@font/poppinssemibold"
            android:paddingVertical="12dp"
            android:stateListAnimator="@null"
            android:text="@string/reenviar_btn_enviar"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            app:backgroundTint="@null" />

        <!-- ¿Ya tienes el código? Activar cuenta -->
        <LinearLayout
            android:id="@+id/layoutIrActivarCuenta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:gravity="center"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/txtYaTienesCodigo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/nunitoregular"
                android:text="@string/reenviar_ya_codigo"
                android:textColor="#555555"
                android:textSize="15sp" />

            <TextView
                android:id="@+id/txtIrActivarDesdeReenviar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:fontFamily="@font/poppinsmedium"
                android:text="@string/reenviar_ir_activar"
                android:textColor="#48C9B0"
                android:textSize="15sp" />
        </LinearLayout>

    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>